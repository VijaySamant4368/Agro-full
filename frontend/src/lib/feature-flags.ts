/**
 * Central feature flags. Flip a flag here to re-enable a stubbed feature everywhere
 * it's gated (page route, nav links, footer links) in one place.
 */

// Safety Matrix: page, nav, and footer links are all gated off this single flag.
export const MATRIX_FEATURE_ENABLED = false;
